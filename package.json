{
  "name": "agent-eval-platform",
  "version": "0.2.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "start:browser": "npm --workspace @agent-eval-platform/browser-runner start",
    "start:dsh": "npm --workspace @agent-eval-platform/dsh-bridge start"
  }
}
